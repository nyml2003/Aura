{
  "name": "@aura/backend",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "prebuild": "pnpm --dir ../contract run build && pnpm --dir ../site run build",
    "prepare-site": "node scripts/prepare-site.cjs",
    "dev": "tsx watch --tsconfig tsconfig.dev.json src/index.ts",
    "predev": "pnpm --dir ../contract run build && pnpm --dir ../site run build && pnpm run prepare-site",
    "prestart": "pnpm --dir ../contract run build && pnpm --dir ../site run build && pnpm run prepare-site"
  },
  "dependencies": {
    "@aura/contract": "file:../contract",
    "@aura/site": "file:../site",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.0"
  },
  "engines": { "node": ">=18" }
}
